import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  top: 10px;
  right: 10px;

  button {
    border: 0;
    background: transparent;

    cursor: pointer;
  }

`;
